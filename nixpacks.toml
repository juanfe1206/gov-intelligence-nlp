[phases.build]
cmds = ["cd backend && pip install -r requirements-dev.txt"]

[phases.setup]
nixPkgs = ["python311", "gcc"]

[start]
cmd = "cd backend && uvicorn app.main:app --host 0.0.0.0 --port ${PORT}"
